name: spotify-ranking
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pip
  - numpy=1.21.2
  - pandas=1.3.3
  - scikit-learn=0.24.2
  - matplotlib=3.4.3
  - pytorch=1.9.0
  - pip:
    - -r file:requirements.txt


# Run the following command after creating the environment:
# conda activate spotify-ranking && conda update -y -n base -c conda-forge conda